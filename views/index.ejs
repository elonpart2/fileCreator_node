<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Sanwal</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post"> 
          <input
            type="text"
            placeholder="Title goes here"
            class="block rounded-md outline-none w-full px-5 py-2 bg-zinc-800"
            required
            name="title"
          />
          <textarea
            placeholder="Task description"
            rows="5"
            name="details"
            class="block rounded-md outline-none w-full mt-2 resize-none px-5 py-2 bg-zinc-800"
          ></textarea>
          <input
            type="submit"
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length > 0){ %> <% files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-3xl tracking-tighter">
            <%=val%>
          </h1>
          <a href="/files/<%=val%>" class="text-blue-500 hover:underline inline-block mt-3"
            >Read more.</a
          >
        </div>
        <%})%> <%} else{%>
        <h1 class="text-zinc-600">no tasks yet.</h1>
        <%} %>
      </div>
    </div>

    <script src="/javascript/script.js"></script>
  </body>
</html>
